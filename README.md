# Marbl

Simple top-down marble tilt demo on a 1024×1024 canvas. WASD tilts the plane and the marble rolls with basic physics.

## Run

Open `marbl.html` in a browser (or `marbld.html` for the level editor).

## Controls

- `W` `A` `S` `D` — tilt the plane; marble rolls with gravity and friction.

